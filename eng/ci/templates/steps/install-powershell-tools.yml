# Install PowerShell and .NET SDKs for PowerShell E2E tests
steps:
- task: UseDotNet@2 # The pinned SDK we use to build
  displayName: Install .NET SDK from global.json
  inputs:
    packageType: sdk
    useGlobalJson: true
