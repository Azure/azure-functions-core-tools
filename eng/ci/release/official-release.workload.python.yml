parameters:
- name: public
  displayName: Publish to nuget.org?
  type: boolean
  default: false

# Release has no triggers outside of pipeline
trigger: none
pr: none

extends:
  template: /eng/ci/templates/pipelines/release-packages.yml@self
  parameters:
    public: ${{ parameters.public }}
    folder: workload/python
    projects: |
      $(Build.SourcesDirectory)/src/Workload/Stacks/Python/Workload.Python.csproj
      $(Build.SourcesDirectory)/test/Workload/Stacks/Python.Tests/Workload.Python.Tests.csproj
