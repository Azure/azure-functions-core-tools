<Project>

  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.targets', '$(MSBuildThisFileDirectory)../'))" />

  <Import Project="$(EngBuildRoot)Workers.Powershell.targets" />
  <Import Project="$(EngBuildRoot)Workers.Python.Workaround.targets" Condition="'$(RuntimeIdentifier)' == 'win-arm64'" />

  <Import Project="$(EngBuildRoot)Distlib.targets" />
  <Import Project="$(EngBuildRoot)Minified.targets" />
  <Import Project="$(EngBuildRoot)Telemetry.targets" />
  <Import Project="$(EngBuildRoot)Templates.targets" />

</Project>