

BaseAzureAction
    -> this accepts --access-token, or --access-token--stdin
    -> If no access token passed on the commandline, it checks OS:
        -> osx/linux:
            -> use 'az'
        -> windows
            -> use 'az'
            -> use 'powershell'
    -> Update armManager

Remove ArmManager:
    Move all the methods to extension methods on the models

