# Azure.Functions.Cli.Workloads.Workers.Go

Content workload that packages the Go worker assets consumed by Azure Functions Core Tools.

Drop worker files into the `content/` folder. They are packed under `content/` in the resulting NuGet package.
