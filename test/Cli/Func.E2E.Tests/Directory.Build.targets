<Project>

  <Import Project="$([MSBuild]::GetPathOfFileAbove('Directory.Build.targets', '$(MSBuildThisFileDirectory)../'))" />

  <Import Project="$(EngBuildRoot)Distlib.targets" />
  <Import Project="$(EngBuildRoot)Templates.targets" />

</Project>