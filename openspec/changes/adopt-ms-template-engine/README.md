# adopt-ms-template-engine

Adopt the Microsoft templating engine (dotnet/templating) as the single CLI-internal engine behind func new, with template.json-format templates delivered via per-stack content workloads. Replaces the V2 jobs/actions engine and the dotnet-new shell-out.
